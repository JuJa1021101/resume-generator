apiVersion: v1
kind: Service
metadata:
  name: ai-resume-service
  namespace: ai-resume-production
  labels:
    app: ai-resume-app
spec:
  selector:
    app: ai-resume-app
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP